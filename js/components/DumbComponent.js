// go grab react from node_modules
import React from 'react'

// this component doesn't have any of it's own state its just for presentation so let's keep it simple!
const Dummy = () => <div>I'm a dummy component!</div>

export default Dummy // send it off